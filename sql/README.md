# SQL Server work

The final SQL report work is performed separately in Microsoft SQL Server.
The group will add its SQL scripts and screenshots here. The Python notebook
does not claim that the SQL Server portion is complete.
